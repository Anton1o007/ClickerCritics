export type Game = {
    id: string
    title: string
    description: string
    release_date: Date
    genres: string[]
    platforms: string[]
    age_rating: string
    developer: string
    publisher: string
    cover: string
    images: string[]
    videos: string[]
}
