import React from 'react';
import { Update as UpdateContainer } from '@/components/Update';
import { Container } from './styles';

const Register = () => (
  <Container>
    <UpdateContainer />
  </Container>
);

export default Register;